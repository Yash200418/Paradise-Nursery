import {createSlice} from '@reduxjs/toolkit';const slice=createSlice({name:'cart',initialState:{items:[]},reducers:{addItem:(s,a)=>{const i=s.items.find(x=>x.id===a.payload.id);i?i.quantity++:s.items.push({...a.payload,quantity:1})},removeItem:(s,a)=>{s.items=s.items.filter(i=>i.id!==a.payload)},updateQuantity:(s,a)=>{const i=s.items.find(x=>x.id===a.payload.id);if(i&&a.payload.quantity>0)i.quantity=a.payload.quantity}}});export const {addItem,removeItem,updateQuantity}=slice.actions;export default slice.reducer;